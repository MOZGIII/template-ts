{
  "name": "&example-cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "dev": "node src/main.ts",
    "example-cli:dev": "yarn dev",
    "eslint:fix": "eslint . --fix",
    "eslint:check": "eslint .",
    "tsc:check": "tsc --build --noEmit"
  },
  "dependencies": {
    "&core-lib": "workspace:"
  },
  "devDependencies": {
    "&eslint-config": "workspace:",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
